# Compact images are kept at roughly twice the intended model input size.
image_width = 400
image_height = 200
workers = 8

# Change these numbers whenever you want a smaller or larger corpus.
[datasets]
wider_face_train = 12880
wider_face_validation = 3226
scut_head_train = 3405
scut_head_validation = 1000
crowdhuman_train = 15000
crowdhuman_validation = 4370
open_images_train_negatives = 5000
open_images_validation_negatives = 1000
